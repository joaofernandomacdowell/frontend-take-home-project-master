import React from 'react';
import { Header, Search, Countries } from '../sections';

const App = (): JSX.Element => (
  <>
    <Header />
    <Search />
    <Countries />
  </>
);

export default App;
